{
  "$schema": "https://opencode.ai/config.json",

  // === AUTO-LOADED MEMORY ===
  // Every file listed here is injected into every agent session automatically.
  // Add/remove paths as needed — supports glob patterns.
  "instructions": [".opencode/instructions/memory.md"]
}
